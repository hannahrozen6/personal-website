[build]
  command = """
  bash -lc 'QUARTO_VERSION=1.4.555
  curl -L "https://github.com/quarto-dev/quarto-cli/releases/download/v${QUARTO_VERSION}/quarto-${QUARTO_VERSION}-linux-amd64.tar.gz" | tar -xz -C "$HOME"
  export PATH="$HOME/quarto/bin:$PATH"
  quarto render'
  """
  publish = "_site"